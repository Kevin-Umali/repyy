// Package scan orchestrates read-only repository inspection. It owns traversal,
// rule matching, finding construction, and evidence policy. Parsers with a
// distinct input domain live in the media, supplychain, and workflow packages.
package scan
